# Dependências
node_modules/
.pnpm-store/

# Builds
.next/
.open-next/
dist/
build/
.turbo/
*.tsbuildinfo

# Ambiente — NUNCA commitar segredos
.env
.env.*
!.env.example

# Prisma
*.db
*.db-journal
packages/db/src/generated/

# Logs e temporários
*.log
npm-debug.log*
pnpm-debug.log*
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Cloudflare / OpenNext
.wrangler/
.dev.vars*
# Cópias das URLs hospedadas, guardadas ao migrar o desenvolvimento para local.
# O padrão acima precisa do curinga: `.dev.vars` casa só com o nome exato, e
# `.dev.vars.neon` — que carrega a credencial do Neon — passou batido por isso.
*.neon
.open-next/

# Testes
coverage/
